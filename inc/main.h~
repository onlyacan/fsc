#ifndef __MAIN_H__
#define __MAIN_H__

#include <stdio.h>
#include <math.h>
#include <stdlib.h>


#define DIM 2  // in this case all vec 2d
#define STRLEN 512  // 


enum Boolean {FALSE = 0, TRUE = 1}; 

#endif /* __MAIN_H__ */
